# waybar は systemd (waybar.service) で管理するため Sway 内蔵 bar を無効化する。
# システム版 90-bar.conf を同名で上書きし、bar{} を置かないことで実現している。
# このファイルを消すとシステム版が有効になり swaybar が起動してしまう。
# 詳細は ~/.dotfiles/CLAUDE.md の「常駐ツールの起動元マップ」を参照。
