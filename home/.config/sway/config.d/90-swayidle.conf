# swayidle / gtklock は systemd (swayidle.service → scripts/swayidle-run.sh) で管理する。
# このファイルはシステム版 90-swayidle.conf を同名で上書きして「exec しない」ためのもの。
# 消すとシステム版が有効になり swayidle が二重起動する。
# ロック画面のテーマは ~/.config/gtklock/。
# 詳細は ~/.dotfiles/CLAUDE.md の「常駐ツールの起動元マップ」を参照。
