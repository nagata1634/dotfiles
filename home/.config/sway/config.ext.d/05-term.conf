# $term を自作ラッパー(foot の server/client 構成)に差し替える。
# Sway の変数は定義時に展開されるため、コア側で既に展開済みの $mod+Return には
# 反映されない。よって --no-warn 付きで再定義する。
# 理由は ~/.dotfiles/CLAUDE.md の「sway config のコア / 拡張分離」を参照。
set $term $scripts/term.sh
bindsym --no-warn $mod+Return exec $term
