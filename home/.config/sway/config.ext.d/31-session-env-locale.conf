# ロケールを単一の源 ~/.config/locale.env から読み直して伝播し直す。
# コア側 config.d/30-session-env.conf の後に実行され、その値を上書きする。
# なぜ必要か(lang.sh の CJK 置換)は ~/.dotfiles/CLAUDE.md の「ロケール分離」を参照。
exec $scripts/sway-env-propagate.sh
