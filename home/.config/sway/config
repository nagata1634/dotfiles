# Default config for sway
#
# Copy this to ~/.config/sway/config and edit it to your liking.
#
# Read `man 5 sway` for a complete reference.

### Variables
#
# Logo key. Use Mod1 for Alt.
set $mod Mod4
# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l
# Your preferred terminal emulator
# コアは素の foot。自作スクリプトを入れている環境では config.ext.d/05-term.conf が
# term.sh（foot の server/client 構成）に差し替える。設定は ~/.config/foot/foot.ini。
set $term foot
# 自作スクリプト置き場。config.d / config.ext.d からも参照できる。
set $scripts $HOME/.config/sway/scripts
# Your preferred application launcher
# Note: pass the final command to swaymsg so that the resulting window can be opened
# on the original workspace that the command was run on.
# Recommends: rofi-wayland
set $rofi_cmd rofi \
        -terminal '$term'
# Mod+d: アプリ（.desktopファイル）のみを表示するランチャー
set $menu $rofi_cmd -show drun -modes drun

### Output configuration
#
# Default wallpaper (more resolutions are available in /usr/share/backgrounds/sway/)
# Requires: desktop-backgrounds-compat, swaybg, jxl-pixbuf-loader
# 既定の壁紙＝壁紙ピッカーが生成する内蔵用画像（eDP-1 はこれを1枚で表示）。
# 外部モニタ(DP)は config.d/10-outputs.conf で span 用の dp6/dp5.png に上書きされる。
output * bg $HOME/Pictures/background/.span/edp1.png fill
#
# Example configuration:
#
#   output HDMI-A-1 resolution 1920x1080 position 1920,0
#
# You can get the names of your outputs by running: swaymsg -t get_outputs

### Idle configuration
#
# Example configuration:
#
# exec swayidle -w \
#          timeout 300 'swaylock -f -c 000000' \
#          timeout 600 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
#          before-sleep 'swaylock -f -c 000000'
#
# This will lock your screen after 300 seconds of inactivity, then turn off
# your displays after another 300 seconds, and turn your screens back on when
# resumed. It will also lock your screen before your computer goes to sleep.

### Input configuration
#
# Example configuration:
#
#   input "2:14:SynPS/2_Synaptics_TouchPad" {
#       dwt enabled
#       tap enabled
#       natural_scroll enabled
#       middle_emulation enabled
#   }
#
# You can get the names of your inputs by running: swaymsg -t get_inputs
# Read `man 5 sway-input` for more information about this section.

### Key bindings
#
# 各 bindsym の直前行に `#: 機能名` を置くと、keybind-rofi.py の一覧に
# その機能名が表示される（i3/Sway は行末インラインコメント不可なので前の行に書く）。
#
# Basics:
#
    # Start a terminal
    #: ターミナルを起動
    bindsym $mod+Return exec $term

    # Kill focused window
    #: ウィンドウを閉じる
    bindsym $mod+Shift+q kill

    # Start your launcher（$menu = 上で定義した rofi -show drun。XIM経由で日本語入力可）
    #: アプリランチャー（rofi・日本語検索可）
    bindsym $mod+d exec $menu

    # $mod+Tab / $mod+Shift+Tab（rofi でウィンドウ・ワークスペース切替）は
    # 自作スクリプト依存なので config.ext.d/10-script-bindings.conf へ移設。

    # Drag floating windows by holding down $mod and left mouse button.
    # Resize them with right mouse button + $mod.
    # Despite the name, also works for non-floating windows.
    # Change normal to inverse to use left mouse button for resizing and right
    # mouse button for dragging.
    floating_modifier $mod normal

    # Reload the configuration file
    #: Sway 設定を再読み込み
    bindsym $mod+Shift+c reload

    # Exit sway (logs you out of your Wayland session)
    #: Sway を終了（確認ダイアログあり）
    bindsym $mod+Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -B 'Yes, exit sway' 'swaymsg exit'
#
# Moving around:
#
    # Move your focus around
    #: フォーカス移動 ←
    bindsym $mod+$left focus left
    #: フォーカス移動 ↓
    bindsym $mod+$down focus down
    #: フォーカス移動 ↑
    bindsym $mod+$up focus up
    #: フォーカス移動 →
    bindsym $mod+$right focus right
    # Or use $mod+[up|down|left|right]
    #: フォーカス移動 ←
    bindsym $mod+Left focus left
    #: フォーカス移動 ↓
    bindsym $mod+Down focus down
    #: フォーカス移動 ↑
    bindsym $mod+Up focus up
    #: フォーカス移動 →
    bindsym $mod+Right focus right

    # Move the focused window with the same, but add Shift
    #: ウィンドウ移動 ←
    bindsym $mod+Shift+$left move left
    #: ウィンドウ移動 ↓
    bindsym $mod+Shift+$down move down
    #: ウィンドウ移動 ↑
    bindsym $mod+Shift+$up move up
    #: ウィンドウ移動 →
    bindsym $mod+Shift+$right move right
    # Ditto, with arrow keys
    #: ウィンドウ移動 ←
    bindsym $mod+Shift+Left move left
    #: ウィンドウ移動 ↓
    bindsym $mod+Shift+Down move down
    #: ウィンドウ移動 ↑
    bindsym $mod+Shift+Up move up
    #: ウィンドウ移動 →
    bindsym $mod+Shift+Right move right
#
# Workspaces:
#
    # Switch to workspace ($mod+数字) は config.d/17-workspace-goto.conf へ移設。
    #   絶対マッピング+フォーカス追従（奇数キー→右画面 / 偶数キー→左画面）。
    #   ここに旧バインドを残すと早見表(keybind-rofi)に重複表示されるため撤去した。
    # Move focused container to workspace
    #: ワークスペース 1 へ移動
    bindsym $mod+Shift+1 move container to workspace number 1
    #: ワークスペース 2 へ移動
    bindsym $mod+Shift+2 move container to workspace number 2
    #: ワークスペース 3 へ移動
    bindsym $mod+Shift+3 move container to workspace number 3
    #: ワークスペース 4 へ移動
    bindsym $mod+Shift+4 move container to workspace number 4
    #: ワークスペース 5 へ移動
    bindsym $mod+Shift+5 move container to workspace number 5
    #: ワークスペース 6 へ移動
    bindsym $mod+Shift+6 move container to workspace number 6
    #: ワークスペース 7 へ移動
    bindsym $mod+Shift+7 move container to workspace number 7
    #: ワークスペース 8 へ移動
    bindsym $mod+Shift+8 move container to workspace number 8
    #: ワークスペース 9 へ移動
    bindsym $mod+Shift+9 move container to workspace number 9
    #: ワークスペース 10 へ移動
    bindsym $mod+Shift+0 move container to workspace number 10
    # Note: workspaces can have any name you want, not just numbers.
    # We just use 1-10 as the default.

    # Outputs (monitors): DP-5=左(横) / DP-6=右(縦)  ※実設定は config.d/10-outputs.conf
    # フォーカスを隣のモニターへ移動（左右・上下）
    #: 別モニタにフォーカス ←
    bindsym $mod+Ctrl+Left   focus output left
    #: 別モニタにフォーカス →
    bindsym $mod+Ctrl+Right  focus output right
    #: 別モニタにフォーカス ↑
    bindsym $mod+Ctrl+Up     focus output up
    #: 別モニタにフォーカス ↓
    bindsym $mod+Ctrl+Down   focus output down
    #: 別モニタにフォーカス ←
    bindsym $mod+Ctrl+$left  focus output left
    #: 別モニタにフォーカス →
    bindsym $mod+Ctrl+$right focus output right
    #: 別モニタにフォーカス ↑
    bindsym $mod+Ctrl+$up    focus output up
    #: 別モニタにフォーカス ↓
    bindsym $mod+Ctrl+$down  focus output down
    # フォーカス中のウィンドウを隣のモニターへ移動（左右・上下）
    #: 別モニタへ移動 ←
    bindsym $mod+Ctrl+Shift+Left   move container to output left
    #: 別モニタへ移動 →
    bindsym $mod+Ctrl+Shift+Right  move container to output right
    #: 別モニタへ移動 ↑
    bindsym $mod+Ctrl+Shift+Up     move container to output up
    #: 別モニタへ移動 ↓
    bindsym $mod+Ctrl+Shift+Down   move container to output down
    #: 別モニタへ移動 ←
    bindsym $mod+Ctrl+Shift+$left  move container to output left
    #: 別モニタへ移動 →
    bindsym $mod+Ctrl+Shift+$right move container to output right
    #: 別モニタへ移動 ↑
    bindsym $mod+Ctrl+Shift+$up    move container to output up
    #: 別モニタへ移動 ↓
    bindsym $mod+Ctrl+Shift+$down  move container to output down
    # モニタ役割入替 / キーバインド一覧 / 電源メニュー / 設定ハブ / 通知センターは
    # いずれも自作スクリプト依存なので config.ext.d/10-script-bindings.conf へ移設。
#
# Layout stuff:
#
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
    #: 横に分割（左右に並べる）
    bindsym $mod+b splith
    #: 縦に分割（上下に並べる）
    bindsym $mod+v splitv

    # Switch the current container between different layout styles
    #: スタックレイアウト
    bindsym $mod+s layout stacking
    #: タブレイアウト
    bindsym $mod+w layout tabbed
    #: 分割レイアウト（横/縦）を切替
    bindsym $mod+e layout toggle split

    # Make the current focus fullscreen
    #: フルスクリーン切替
    bindsym $mod+f fullscreen

    # Toggle the current focus between tiling and floating mode
    #: フローティング切替
    bindsym $mod+Shift+space floating toggle

    # Swap focus between the tiling area and the floating area
    #: タイル/フローティング のフォーカス切替
    bindsym $mod+space focus mode_toggle

    # Move focus to the parent container
    #: 親コンテナにフォーカス
    bindsym $mod+a focus parent
    # Move focus to the child container
    #: 子コンテナにフォーカス
    bindsym $mod+Shift+a focus child
#
# Scratchpad:
#
    # Sway has a "scratchpad", which is a bag of holding for windows.
    # You can send windows there and get them back later.

    # Move the currently focused window to the scratchpad
    # （minus はズーム系と衝突するため grave=バックティックに変更）
    #: スクラッチパッドへ退避
    bindsym $mod+Shift+grave move scratchpad

    # スクラッチパッドの rofi 一覧表示（$mod+grave）は自作スクリプト依存なので
    # config.ext.d/10-script-bindings.conf へ移設。
    # 固定Fキー割当と FIFO巡回は config.d/17-scratchpad.conf 側（スクリプト非依存）。
#
# Resizing containers:
#
mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    #: [リサイズ] 幅を縮小
    bindsym $left resize shrink width 10px
    #: [リサイズ] 高さを拡大
    bindsym $down resize grow height 10px
    #: [リサイズ] 高さを縮小
    bindsym $up resize shrink height 10px
    #: [リサイズ] 幅を拡大
    bindsym $right resize grow width 10px

    # Ditto, with arrow keys
    #: [リサイズ] 幅を縮小
    bindsym Left resize shrink width 10px
    #: [リサイズ] 高さを拡大
    bindsym Down resize grow height 10px
    #: [リサイズ] 高さを縮小
    bindsym Up resize shrink height 10px
    #: [リサイズ] 幅を拡大
    bindsym Right resize grow width 10px

    # Return to default mode
    #: [リサイズ] 終了
    bindsym Return mode "default"
    #: [リサイズ] 終了
    bindsym Escape mode "default"
}
#: リサイズモードに入る
bindsym $mod+r mode "resize"

# Include configs from 3 locations:
#  - /usr/share/sway/config.d
#  - /etc/sway/config.d
#  - $XDG_CONFIG_HOME/sway/config.d ($HOME/.config/sway/config.d)
#
# If multiple directories contain the files with the same name, the later
# directory takes precedence; `$XDG_CONFIG_HOME/sway/config.d/20-swayidle.conf`
# will always be loaded instead of `/usr/share/sway/config.d/20-swayidle.conf`
# or `/etc/sway/config.d/20-swayidle.conf`
#
# This mechanism permits overriding our default configuration per-system
# (/etc) or per-user ($XDG_CONFIG_HOME) basis. Just create the file you
# want to modify/override in the higher-level directory.
#
# For example, to disable the default bar from Fedora configs, you'll need to
#     $ echo -n > "$HOME/.config/sway/config.d/90-bar.conf"
#
# Note the quoting, the $() and the arguments quoting. All the parts are equally
# important to make the magic work. And if you want to learn the secret behind
# the trick, it's all in the `wordexp(3)`.
#
include '$(/usr/libexec/sway/layered-include "/usr/share/sway/config.d/*.conf" "/etc/sway/config.d/*.conf" "${XDG_CONFIG_HOME:-$HOME/.config}/sway/config.d/*.conf")'

# 自作スクリプト依存の拡張設定。ここまでが「素の Sway でも動くコア」で、
# 以降は scripts/ がある環境だけで有効になる。scripts/ を入れていない環境では
# ディレクトリが無いだけで、layered-include はマッチしないパスを黙って無視する。
include '$(/usr/libexec/sway/layered-include "${XDG_CONFIG_HOME:-$HOME/.config}/sway/config.ext.d/*.conf")'
