# GUI(sway)セッション用ロケールの単一の真実の源。
# 読み込み先・TTY を英語に保つ仕組み・なぜ必要かは
# ~/.dotfiles/CLAUDE.md の「ロケール分離」を参照。検証は `locale-audit`。
#
# 書式は KEY=VALUE のみ。systemd environment.d と `set -a; . file` の
# 両方から読むため、クォート・変数展開・シェル構文は使わないこと。
LANG=ja_JP.UTF-8
LANGUAGE=ja
