# ユーザー定義のエイリアスと関数。
# ~/.bashrc が ~/.bashrc.d/* を読むため、ここに書けば全ての対話シェルで有効になる。
# 先頭の数字は読み込み順（90-tty-locale.sh より先に読まれる）。
#
# 環境固有のもの（特定のコンテナやホストに依存する関数など）はここに書かず、
# dotfiles 管理外の別ファイルとして ~/.bashrc.d/ に直接置くこと。
#
# 例:
# alias ll='ls -alF'
